# dependencies
node_modules/

# next.js
.next/
out/

# env files — never commit credentials
.env.local
.env*.local

# debug
npm-debug.log*

# misc
.DS_Store
*.pem

# local PR counter — tracked locally only, causes merge conflicts when in git
.pr-counter
tsconfig.tsbuildinfo

# Local-only working docs (PR proposals, design scratchpads). Never committed —
# mirrors the policy in mu-wp-staging. Backfill the real PR number into the local
# doc when its PR merges.
docs/
